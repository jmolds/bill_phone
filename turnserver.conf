# Minimal coturn config for Docker
listening-port=3478
fingerprint
lt-cred-mech
realm=billphone-app
user=webrtcuser88:supersecret88
total-quota=100
bps-capacity=0
stale-nonce
no-loopback-peers
no-multicast-peers
# Allow all IPs (for dev). For production, restrict as needed.
# External IP will be set by Docker's network mode.
